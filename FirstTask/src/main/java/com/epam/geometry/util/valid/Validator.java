package com.epam.geometry.util.valid;

import com.epam.geometry.util.impl.ValidatorImpl;

public class Validator implements ValidatorImpl {

    public boolean isValid() {
        return false;
    }
}
